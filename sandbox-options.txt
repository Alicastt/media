VERSION = 1,

option DMM.ChanceOfEvent
{
    type = integer,
    min = 0,
    max = 100,
    default = 12,
    page = DMM,
    translation = DMM_ChanceOfEvent,
}

option DMM.ZombieRadius
{
    type = integer,
    min = 1,
    max = 200,
    default = 10,
    page = DMM,
    translation = DMM_ZombieRadius,
}

option DMM.MinAmountZombie
{
    type = integer,
    min = 10,
    max = 500,
    default = 200,
    page = DMM,
    translation = DMM_MinAmountZombie,
}
option DMM.MaxAmountZombie
{
    type = integer,
    min = 10,
    max = 500,
    default = 200,
    page = DMM,
    translation = DMM_MaxAmountZombie,
}

option DMM.MaxAmountSpawnedCars
{
    type = integer,
    min = 1,
    max = 4,
    default = 2,
    page = DMM,
    translation = DMM_MaxAmountSpawnedCars,
}